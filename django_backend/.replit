run = "bash start.sh"

[onBoot]
command = """
if [ ! -d .venv ]; then uv venv; fi && uv pip install -r requirements.txt
"""

[deployment]
run = ["bash", "start.sh"]

[[ports]]
localPort = 8000
externalPort = 8000

[nix]
channel = "stable-24_05"
